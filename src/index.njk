---
layout: html-base
title: home
---

<main class="blog__home">
  <div class="stack">
    {%- for post in collections.blogPosts -%}
      <article class="blog">
        <header>
          <h2 class="blog__title">
            <a class="blog__link" href="{{ post.url | url }}">{{ post.data.title }}</a>
          </h2>
        </header>

        <div>
          <p class="blog__description">{{ post.data.summary }}</p>
          <span class="blog__description-hr"/>
        </div>

        <p class="blog__meta">
          <span class="blog__by">posted on</span>
          <time class="blog__date">{{ post.data.createdDate | dateFilter }}</time>
          <span class="blog__by">and updated</span>
          <time class="blog__date">{{ post.data.updatedDate }}</time>
        </p>
      </article>
    {%- endfor -%}

    {% include "footer.njk" %}
  </div>
</main>
